{
  "name": "maxos",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd app && npm run dev\" \"cd orchestrator && npm run dev\"",
    "install:all": "npm install --prefix app && npm install --prefix orchestrator"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
